apiVersion: v1
kind: Service
metadata:
  name: {{ include "rc_all.fullname" . }}
  labels:
    {{- include "rc_all.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rc_all.selectorLabels" . | nindent 4 }}
